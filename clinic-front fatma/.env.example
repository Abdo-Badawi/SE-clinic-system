# Copy this file to .env and fill in your values
# If using Vite proxy (default), leave VITE_API_URL empty — the proxy in vite.config.ts handles it
# If deploying to production without the proxy, set the full backend URL:
VITE_API_URL=http://localhost:8080
